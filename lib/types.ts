export interface ZoneType{
    id: string;
    name: string;
    code: string;
    active: boolean;
}
export interface StateType {
    id: string;
    name: string;
    code: string;
    active: boolean;
}